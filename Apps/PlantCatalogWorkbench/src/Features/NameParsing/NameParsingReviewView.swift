import SwiftUI

struct NameParsingReviewView: View {
    var body: some View {
        ContentUnavailableView(
            "Name Parsing",
            systemImage: "text.magnifyingglass",
            description: Text("Batch parsing, confidence review, and writeback controls will be added here.")
        )
    }
}
